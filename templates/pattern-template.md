# Pattern: {Name}

**Tags**: backend | frontend | data | infra | security | testing — pick relevant
**Status**: Active | Deprecated (in favor of [[other-pattern]])

## When to use
Conditions under which this pattern applies. Be specific — "always" is not a condition.

## When NOT to use
Anti-conditions. Common misuses. Where it's overkill.

## Implementation

```{language}
// minimal, runnable reference implementation
// keep it short — the pattern is the abstraction, not the example
```

## Notes
- **Performance**: any cost characteristics worth knowing
- **Security**: any implications (auth, input handling, secret exposure)
- **Operational**: failure modes, observability hooks needed
- **Related patterns**: [[pattern-name]] for adjacent cases; [[other-pattern]] as a fallback

## References
- Link to the story or DEC that introduced this pattern.
- External canonical source (Fowler, GoF, framework docs, etc.) if applicable.

---

> Save this file under `.claude/memory/patterns/{pattern-slug}.md`.
> Patterns are read by `backend`, `frontend`, `architect` (DESIGN), and `clarify` (PLAN) before they invent new approaches.
> Curate this directory deliberately — every entry should be one you'd recommend to a new team member without hesitation.
